Flask==3.0.3
flask-cors==4.0.1

# ROS2 integration (recommended approach): connect to ROS2 topics via rosbridge websocket.
# Requires a running rosbridge server (typically on port 9090).
roslibpy>=1.7.0
